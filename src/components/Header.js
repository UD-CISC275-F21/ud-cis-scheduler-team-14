import React from 'react'

const Header = ({onAdd}) => {
    return (
        <nav>
            <header className="navbar navbar-light bg-light">four year plan
            <button className ="btn btn-primary" >Clear</button>
            <button className ="btn btn-primary" >Load</button>
            </header>
        </nav>
    )
}

export default Header
